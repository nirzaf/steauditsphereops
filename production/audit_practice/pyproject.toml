[project]
name = "audit-practice"
authors = [{ name = "STE AuditSphere Ops", email = "info@quadrate.lk" }]
description = "STE AuditSphere Ops custom Frappe application."
readme = "README.md"
requires-python = ">=3.14,<3.15"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core==3.12.0"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = "==16.34.0"
erpnext = "==16.35.0"
